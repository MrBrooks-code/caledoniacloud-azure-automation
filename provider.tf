terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~>3.0"
    }
  }
}

provider "azurerm" {
  features {}
  
  # Automatically uses az login credentials
  # No additional configuration needed if you're logged in via Azure CLI
}